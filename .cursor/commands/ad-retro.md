Run only:
- Stage 7 retrospective-writer
- Stage 8 memory-manager

Requirements:
- Use approved outputs from earlier stages as input
- Use workflow_schema_version = adwf.v1
- Identify:
  - what worked well
  - workflow weaknesses
  - schema drift
  - prompt/rule improvements
  - memory/schema improvements
- memory_updates must be structured objects only

Use the schema defined in:
- workflow/schema/workflow_schema_v1.json
- workflow/schema/stage_machine_output_contracts.md
